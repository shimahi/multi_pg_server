apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
   - port: 5432
  selector:
   app: postgres

# 4. kubectl create -f postgres-service.yaml
# 5. get svc postgres
# → PORT(S)の 5432:<32412>/TCP の<>を控える
# 5. psql -h localhost -U postgresadmin --password -p 32412 postgresdb
